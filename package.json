{
  "name": "@cucu/keycloak-m2m",
  "version": "0.0.1",
  "description": "Keycloak M2M helpers for NestJS (token cache + outbound interceptor + inbound guard).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist node_modules"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/passport": "^11.0.5",
    "passport-jwt": "^4.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "eslint": "^9.10.0",
    "@typescript-eslint/eslint-plugin": "^8.4.0",
    "@typescript-eslint/parser": "^8.4.0",
    "@types/passport-jwt": "^4.0.1"
  }
}